<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.session.mapper.SessionMapper">
    
    <resultMap type="Session" id="SessionResult">
        <result property="id"    column="id"    />
        <result property="competitionId"    column="competition_id"    />
        <result property="session"    column="session"    />
        <result property="category"    column="category"    />
        <result property="organizations"    column="organizations"    />
        <result property="level"    column="level"    />
        <result property="status"    column="status"    />
        <result property="createBy"    column="create_by"    />
        <result property="createTime"    column="create_time"    />
        <result property="updateBy"    column="update_by"    />
        <result property="updateTime"    column="update_time"    />
        <result property="remark"    column="remark"    />
        <result property="delFlag"    column="del_flag"    />
    </resultMap>

    <resultMap id="SessionTagResult" type="Session" extends="SessionResult">
        <collection property="tagList" ofType="Tag" column="id" select="selectTagList" />
    </resultMap>

    <resultMap type="Tag" id="TagResult">
        <result property="id"    column="id"    />
        <result property="competitionSessionId"    column="competition_session_id"    />
        <result property="tagName"    column="tag_name"    />
        <result property="depId"    column="dep_id"    />
        <result property="createBy"    column="create_by"    />
        <result property="createTime"    column="create_time"    />
        <result property="updateBy"    column="update_by"    />
        <result property="updateTime"    column="update_time"    />
        <result property="remark"    column="remark"    />
        <result property="delFlag"    column="del_flag"    />
    </resultMap>

    <sql id="selectSessionVo">
        select id, competition_id, session, category, organizations, level, status, create_by, create_time, update_by, update_time, remark, del_flag from session
    </sql>

    <select id="selectSessionList" parameterType="Session" resultMap="SessionResult">
        <include refid="selectSessionVo"/>
        <where>  
            <if test="competitionId != null "> and competition_id = #{competitionId}</if>
            <if test="session != null  and session != ''"> and session = #{session}</if>
            <if test="category != null  and category != ''"> and category = #{category}</if>
            <if test="organizations != null  and organizations != ''"> and organizations like concat('%', #{organizations}, '%')</if>
            <if test="level != null  and level != ''"> and level = #{level}</if>
            <if test="status != null  and status != ''"> and status = #{status}</if>
        </where>
    </select>
    
    <select id="selectSessionById" parameterType="Long" resultMap="SessionTagResult">
        select id, competition_id, session, category, organizations, level, status, create_by, create_time, update_by, update_time, remark, del_flag
        from session
        where id = #{id}
    </select>

    <select id="selectTagList" resultMap="TagResult">
        select id, competition_session_id, tag_name, dep_id, create_by, create_time, update_by, update_time, remark, del_flag
        from tag
        where competition_session_id = #{competition_session_id}
    </select>

    <insert id="insertSession" parameterType="Session" useGeneratedKeys="true" keyProperty="id">
        insert into session
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="competitionId != null">competition_id,</if>
            <if test="session != null and session != ''">session,</if>
            <if test="category != null and category != ''">category,</if>
            <if test="organizations != null and organizations != ''">organizations,</if>
            <if test="level != null">level,</if>
            <if test="status != null">status,</if>
            <if test="createBy != null">create_by,</if>
            <if test="createTime != null">create_time,</if>
            <if test="updateBy != null">update_by,</if>
            <if test="updateTime != null">update_time,</if>
            <if test="remark != null">remark,</if>
            <if test="delFlag != null">del_flag,</if>
         </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="competitionId != null">#{competitionId},</if>
            <if test="session != null and session != ''">#{session},</if>
            <if test="category != null and category != ''">#{category},</if>
            <if test="organizations != null and organizations != ''">#{organizations},</if>
            <if test="level != null">#{level},</if>
            <if test="status != null">#{status},</if>
            <if test="createBy != null">#{createBy},</if>
            <if test="createTime != null">#{createTime},</if>
            <if test="updateBy != null">#{updateBy},</if>
            <if test="updateTime != null">#{updateTime},</if>
            <if test="remark != null">#{remark},</if>
            <if test="delFlag != null">#{delFlag},</if>
         </trim>
    </insert>

    <update id="updateSession" parameterType="Session">
        update session
        <trim prefix="SET" suffixOverrides=",">
            <if test="competitionId != null">competition_id = #{competitionId},</if>
            <if test="session != null and session != ''">session = #{session},</if>
            <if test="category != null and category != ''">category = #{category},</if>
            <if test="organizations != null and organizations != ''">organizations = #{organizations},</if>
            <if test="level != null">level = #{level},</if>
            <if test="status != null">status = #{status},</if>
            <if test="createBy != null">create_by = #{createBy},</if>
            <if test="createTime != null">create_time = #{createTime},</if>
            <if test="updateBy != null">update_by = #{updateBy},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
            <if test="remark != null">remark = #{remark},</if>
            <if test="delFlag != null">del_flag = #{delFlag},</if>
        </trim>
        where id = #{id}
    </update>

    <delete id="deleteSessionById" parameterType="Long">
        delete from session where id = #{id}
    </delete>

    <delete id="deleteSessionByIds" parameterType="String">
        delete from session where id in 
        <foreach item="id" collection="array" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
    
    <delete id="deleteTagByCompetitionSessionIds" parameterType="String">
        delete from tag where competition_session_id in 
        <foreach item="competitionSessionId" collection="array" open="(" separator="," close=")">
            #{competitionSessionId}
        </foreach>
    </delete>

    <delete id="deleteTagByCompetitionSessionId" parameterType="Long">
        delete from tag where competition_session_id = #{competitionSessionId}
    </delete>

    <insert id="batchTag">
        insert into tag( id, competition_session_id, tag_name, dep_id, create_by, create_time, update_by, update_time, remark, del_flag) values
        <foreach item="item" index="index" collection="list" separator=",">
            ( #{item.id}, #{item.competitionSessionId}, #{item.tagName}, #{item.depId}, #{item.createBy}, #{item.createTime}, #{item.updateBy}, #{item.updateTime}, #{item.remark}, #{item.delFlag})
        </foreach>
    </insert>
</mapper>